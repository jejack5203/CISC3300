CISC 3300 F24
In class assignment 3

1. The HTML __id__ attribute is used to specify a unique __id__ for an HTML element

2. The HTML ___<div>___ tag is a block-level element used for grouping and structuring content.

3. CSS ___declarations___ have a property and a value and apply a visual effect.

4. CSS __selectors___ are used to "find" (or select) the HTML elements you want to style

5. If there are two or more CSS rules that point to the same element, the selector with the
highest __specificity_____ will "win".

6. HTML treats every element as living in its own __box____.

7. The ___padding____ property allows you to specify how much space should appear between the
content of an element and its border

8. ___text align center___ is useful for centering inline block elements

9. Setting a CSS rule of display: __block element___ will cause it to start on a new line, and takes up the
whole width
