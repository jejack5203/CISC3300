1. What is HTML? What does it do?

HTML is a markup language which uses a set of rules to define the layout and presentation of texts and images in a digital document. It allows structuring documents, adding formats, and specifying how elements should be displayed on a webpage.


2. Access https://www.fordham.edu/. In your own words, describe how this webpage uses
structure to convey meaning and navigation options to the user.

The webpage includes different sections that separate the information displayed; this makes it easier for the user to interpret the information on the webpage. The top navigation bar gives the user a simple way to explore the different features on the webpage through a list of options.

3. What is an HTML element? Tag? Attribute?

HTML elements are building blocks of webpages by representing headings, paragraphs, links, and images. It is a collection of start and end tags with content inserted between them. HTML tags act like containers; an opening tag, i.e., <h1>, and a closing tag has a forward slash, i.e., </h1>. Tags transform the content enclosed in them. Attributes tell more about the content of elements; they have a name and value.

4. What does it mean when an element has semantic meaning? And structural meaning?

Semantic elements are intended to convey meaning by their element type, even if the element is indistinguishable from other elements visually. Structural elements with meaning have an effect on the structure of a page but don’t necessarily convey any meaning by their element type.

5. What is the default behavior when submitting an HTML form?

The form information will show in the URL.

